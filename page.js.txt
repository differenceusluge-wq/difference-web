import React, { useState } from 'react';
// Za ikone možeš koristiti react-icons (npm install react-icons)
import { FiLayout, FiSmartphone, FiCpu, FiPlus, FiMinus } from 'react-icons/fi';

const LandingPage = () => {
  const [activeFaq, setActiveFaq] = useState(null);

  const usluge = [
    {
      title: "Branding & Logotip",
      desc: "Kreiramo vizualni identitet koji vas izdvaja iz mase i gradi povjerenje kod vaših klijenata.",
      icon: <FiLayout className="text-4xl text-accentGreen" />
    },
    {
      title: "Web Stranice & Aplikacije",
      desc: "Izrada ultra-brzih i responzivnih web rješenja koristeći najmodernije tehnologije poput Reacta.",
      icon: <FiSmartphone className="text-4xl text-accentGreen" />
    },
    {
      title: "Automatizacija Procesa",
      desc: "Povećajte produktivnost integracijom pametnih alata i rješenja koja rade umjesto vas.",
      icon: <FiCpu className="text-4xl text-accentGreen" />
    }
  ];

  const faqs = [
    {
      q: "Koliko traje izrada web stranice?",
      a: "Ovisno o kompleksnosti, prosječna landing stranica je gotova za 7-14 dana, dok kompleksnije aplikacije zahtijevaju 4+ tjedna."
    },
    {
      q: "Nudite li održavanje nakon završetka projekta?",
      a: "Naravno. Svaki klijent dobiva podršku i mogućnost mjesečnog održavanja kako bi sustav bio siguran i ažuran."
    },
    {
      q: "Možete li automatizirati moj postojeći workflow?",
      a: "Da, analiziramo vaše trenutne procese i predlažemo rješenja (npr. Make, Zapier ili custom skripte) za uštedu vremena."
    }
  ];

  return (
    <div className="min-h-screen bg-darkBg text-white selection:bg-accentGreen selection:text-black">
      
      {/* --- HERO SECTION (prethodni kod) --- */}
      {/* ... */}

      {/* --- SEKCIJA USLUGE --- */}
      <section id="usluge" className="py-24 px-8 max-w-7xl mx-auto">
        <div className="text-center mb-16">
          <h2 className="text-4xl font-bold mb-4 uppercase tracking-tighter">Naše Usluge / <span className="text-gray-500 text-2xl font-light italic">Services</span></h2>
          <div className="h-1 w-20 bg-accentGreen mx-auto"></div>
        </div>

        <div className="grid grid-cols-1 md:grid-cols-3 gap-8">
          {usluge.map((usluga, index) => (
            <div key={index} className="group p-8 border border-white/10 bg-white/5 rounded-2xl hover:border-accentGreen/50 transition-all duration-500 hover:-translate-y-2">
              <div className="mb-6 group-hover:scale-110 transition-transform duration-500">{usluga.icon}</div>
              <h3 className="text-xl font-bold mb-4">{usluga.title}</h3>
              <p className="text-gray-400 leading-relaxed">{usluga.desc}</p>
            </div>
          ))}
        </div>
      </section>

      {/* --- FAQ SEKCIJA --- */}
      <section className="py-24 px-8 max-w-4xl mx-auto">
        <div className="text-center mb-16">
          <h2 className="text-3xl font-bold mb-4 uppercase tracking-tighter italic">FAQ</h2>
          <p className="text-gray-500 uppercase tracking-widest text-xs">Često postavljana pitanja</p>
        </div>

        <div className="space-y-4">
          {faqs.map((faq, index) => (
            <div key={index} className="border-b border-white/10">
              <button 
                onClick={() => setActiveFaq(activeFaq === index ? null : index)}
                className="w-full flex justify-between items-center py-6 text-left hover:text-accentGreen transition-colors"
              >
                <span className="text-lg font-medium">{faq.q}</span>
                {activeFaq === index ? <FiMinus /> : <FiPlus />}
              </button>
              
              <div className={`overflow-hidden transition-all duration-300 ${activeFaq === index ? 'max-h-40 pb-6' : 'max-h-0'}`}>
                <p className="text-gray-400">
                  {faq.a}
                </p>
              </div>
            </div>
          ))}
        </div>
      </section>

      {/* --- FOOTER (prethodni kod) --- */}
      {/* ... */}

    </div>
  );
};

export default LandingPage;